# Secret used to sign session JWTs. Generate with: openssl rand -base64 32
# Required in production — without it, sessions are signed with a random
# per-process secret and everyone is logged out on every restart.
SESSION_SECRET=
